pipeline {
    agent any

    environment {
        GIT_REPO = 'https://github.com/zineb-chgari/traffic_optimizer.git'
        COMMIT_SHORT = ''
        // DOCKER_USER = credentials('docker-username')  // à configurer plus tard
        // DOCKER_PASSWORD = credentials('docker-password')
    }

    stages {
        stage('Checkout SCM') {
            steps {
                echo "[INFO] Checking out code from Git"
                checkout([$class: 'GitSCM',
                    branches: [[name: '*/main']],
                    userRemoteConfigs: [[url: env.GIT_REPO]]
                ])
                script {
                    COMMIT_SHORT = bat(returnStdout: true, script: '"C:\\Program Files\\Git\\cmd\\git.exe" rev-parse --short HEAD').trim()
                    echo "Current commit: ${COMMIT_SHORT}"
                }
            }
        }

        stage('Check Git & Docker') {
            steps {
                bat '"C:\\Program Files\\Git\\cmd\\git.exe" --version'
                bat '"C:\\Program Files\\Docker\\Docker\\resources\\bin\\docker.exe" --version'
            }
        }

        stage('Build Frontend Docker') {
            steps {
                echo "[INFO] Building frontend Docker image..."
                bat "\"C:\\Program Files\\Docker\\Docker\\resources\\bin\\docker.exe\" build -f frontend/Dockerfile -t traffic_front:${COMMIT_SHORT} frontend"
            }
        }

        stage('Build Backend Docker') {
            steps {
                echo "[INFO] Building backend Docker image..."
                bat "\"C:\\Program Files\\Docker\\Docker\\resources\\bin\\docker.exe\" build -f backend/Dockerfile -t traffic_back:${COMMIT_SHORT} backend"
            }
        }

        stage('Deploy with Ansible (Placeholder)') {
            steps {
                echo "[INFO] Ansible deployment placeholder"
                // Ici tu pourras exécuter ansible-playbook plus tard
            }
        }

        stage('Provision Infra with Terraform (Placeholder)') {
            steps {
                echo "[INFO] Terraform provisioning placeholder"
                // Ici tu pourras exécuter terraform init/apply plus tard
            }
        }

        stage('Deploy to Kubernetes (Placeholder)') {
            steps {
                echo "[INFO] Kubernetes deployment placeholder"
                // Ici tu pourras exécuter kubectl apply plus tard
            }
        }
    }

    post {
        always {
            node {
                echo "[INFO] Cleaning up Docker..."
                bat '"C:\\Program Files\\Docker\\Docker\\resources\\bin\\docker.exe" system prune -f || exit 0'
            }
        }
        success {
            echo "[SUCCESS] Pipeline finished successfully!"
        }
        failure {
            echo "[ERROR] Pipeline failed!"
        }
    }
}
